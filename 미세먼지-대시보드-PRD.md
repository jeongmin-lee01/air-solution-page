# PRD — 전국 시도별 미세먼지 대시보드

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.3 (Draft) |
| 작성일 | 2026-08-19 |
| 제품 형태 | 반응형 웹 |
| 데이터 출처 | 공공데이터포털 — 한국환경공단 에어코리아 |
| 운영 목표 | 개인 프로젝트 / 포트폴리오 |
| 기술 스택 | Next.js + Supabase + Vercel (§15) |

---

## 1. 배경 및 문제 정의

미세먼지 정보를 확인하는 기존 경로(에어코리아 웹/앱, 포털 날씨)는 대부분 **내 위치 기준 단일 지역**에 최적화되어 있다. 그래서 "지금 서울과 부산 중 어디가 더 나쁜가", "대전은 오늘 계속 이 상태였나"처럼 **지역 간 비교**나 **하루 흐름 파악**은 의외로 번거롭다.

본 서비스는 전국 17개 시·도를 한 화면에 칩(chip) 형태로 늘어놓고, 칩을 클릭하면 해당 지역의 현재 농도와 24시간 추이를 즉시 보여준다. 목표는 **"세 번의 클릭 없이 전국을 훑고, 한 번의 클릭으로 한 지역을 깊게 본다"**는 경험이다.

## 2. 목표 (Goals)

| # | 목표 | 측정 기준 |
|---|---|---|
| G1 | 전국 대기질 상태를 한눈에 스캔 | 첫 화면 로드 시 17개 시도의 등급이 스크롤 없이(데스크톱) 인지 가능 |
| G2 | 지역별 상세 확인의 마찰 최소화 | 칩 클릭 → 상세 표시까지 200ms 이내 (사전 로드된 데이터 기준) |
| G3 | 공공 API의 불안정성을 사용자에게 전가하지 않음 | 외부 API 장애 시에도 마지막 정상 데이터 + 시각 표기로 화면 유지 |
| G4 | 데이터 신뢰성 확보 | 화면의 모든 수치에 측정 시각이 명시됨 |

## 3. 비목표 (Non-Goals)

MVP에서 다루지 **않는** 범위를 명시한다. 범위 확장 압력을 방어하기 위한 항목이다.

- 예보 정보 (오늘/내일/모레 예보, 주간 예보)
- 시·군·구 또는 측정소 단위 세분화
- 사용자 계정, 즐겨찾기, 푸시/이메일 알림
- 지도 기반 시각화 (단계구분도 등)
- PM 외 항목(O₃, NO₂, CO, SO₂) 및 통합대기환경지수
- 다국어 지원, 네이티브 앱

## 4. 타깃 사용자 및 시나리오

**주 사용자**: 출퇴근·외출 전 대기질을 확인하는 일반 사용자, 타 지역에 가족이나 관심 지역이 있는 사용자.

| 시나리오 | 사용자 행동 | 서비스가 해결하는 것 |
|---|---|---|
| S1. 아침 스캔 | 페이지를 열고 전체 칩 색상만 훑음 | 클릭 없이 전국 상태 파악 |
| S2. 특정 지역 확인 | "부산" 칩 클릭 | 부모님이 사는 지역의 현재 수치 |
| S3. 흐름 판단 | 상세의 24시간 그래프 확인 | "나빠지는 중인지 나아지는 중인지" 판단 |
| S4. 지역 비교 | 칩을 번갈아 클릭 | 두 지역 중 어디가 나은지 비교 |

## 5. 핵심 사용자 플로우

```
[페이지 진입]
   ↓
[17개 시도 칩 그리드 렌더 — 각 칩에 지역명 + 등급 색상 + PM10 수치]
   ↓  (기본 선택: 서울)
[선택된 시도의 상세 패널 표시]
   ├── 현재 PM10 / PM2.5 농도 + 등급 라벨
   ├── 측정 시각
   └── 24시간 추이 라인 그래프 (PM10 / PM2.5 토글 또는 2계열)
   ↓
[다른 칩 클릭] → 상세 패널만 교체 (전체 리로드 없음)
```

## 6. 기능 요구사항

### 6.1 시도 칩 그리드 (F-01)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-01-1 | 전국 17개 시·도를 칩으로 표시한다. (서울, 부산, 대구, 인천, 광주, 대전, 울산, 세종, 경기, 강원, 충북, 충남, 전북, 전남, 경북, 경남, 제주) | Must |
| F-01-2 | 각 칩은 지역명, 현재 PM10 농도, 등급 색상을 포함한다. | Must |
| F-01-3 | 선택된 칩은 시각적으로 명확히 구분된다. (테두리/명도 변화 — 색상만으로 구분하지 않음) | Must |
| F-01-4 | 데이터 결측 시 칩은 회색 처리하고 수치 대신 `–`를 표시한다. | Must |
| F-01-5 | 정렬 순서는 고정(권역 순)으로 하며, 농도순 정렬은 후순위 기능으로 둔다. | Should |

### 6.2 상세 패널 (F-02)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-02-1 | 선택 시도의 PM10, PM2.5 현재 농도를 ㎍/㎥ 단위로 표시한다. | Must |
| F-02-2 | 각 항목별 4단계 등급(좋음/보통/나쁨/매우나쁨)을 텍스트와 색상으로 함께 표시한다. | Must |
| F-02-3 | 기준 측정 시각을 `2026-08-19 14:00 기준` 형식으로 표시한다. | Must |
| F-02-4 | 등급에 따른 한 줄 행동 가이드를 표시한다. (예: "외출 시 마스크 착용을 권장합니다") | Should |
| F-02-5 | 칩 전환 시 전체 페이지 리로드 없이 패널만 갱신한다. | Must |

### 6.3 24시간 추이 그래프 (F-03)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-03-1 | 최근 24시간의 시간별 시도 평균 농도를 라인 차트로 표시한다. | Must |
| F-03-2 | PM10과 PM2.5를 함께 표시하거나 토글로 전환할 수 있다. | Must |
| F-03-3 | Y축 배경에 등급 구간을 옅은 밴드로 표시해 절대 수치의 의미를 전달한다. | Should |
| F-03-4 | 결측 구간은 선을 잇지 않고 끊어서 표현한다. (보간으로 데이터를 지어내지 않음) | Must |
| F-03-5 | 초기 적재(§7.2 Backfill)로 배포 직후부터 24시간 그래프를 제공한다. Backfill이 불가능한 경우에 한해 확보 구간만 표시하고 "데이터 축적 중" 안내를 노출한다. | Must |
| F-03-6 | 특정 시점 호버/탭 시 해당 시각의 정확한 수치를 툴팁으로 표시한다. | Should |

### 6.4 상태 및 오류 처리 (F-04)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F-04-1 | 초기 로딩 시 스켈레톤 UI를 표시한다. | Should |
| F-04-2 | 자체 API 실패 시 재시도 버튼과 함께 오류 상태를 표시한다. | Must |
| F-04-3 | 외부 API 장애로 최신 데이터가 없을 경우, 마지막 정상 데이터를 표시하되 "N시간 전 데이터" 배지를 노출한다. | Must |
| F-04-4 | 데이터가 1시간 이상 갱신되지 않으면 상단에 안내 배너를 표시한다. | Should |

## 7. 데이터 요구사항

### 7.1 사용 API

**서비스 URL**: `http://apis.data.go.kr/B552584/ArpltnStatsSvc`
(데이터셋: 한국환경공단_에어코리아_대기오염통계 현황)

| 오퍼레이션 | 용도 | 호출 주기 |
|---|---|---|
| `getCtprvnMesureLIst` (시도별 실시간 평균정보 조회) | 17개 시도 평균 농도 수집 | 매시 1회 |

**핵심 설계 근거**: 이 오퍼레이션은 응답 1건에 17개 시도 값을 모두 담아 반환한다. 따라서 **시간당 1회 호출로 전 지역이 커버**되며, 하루 24회(항목별 2회 호출 시 48회)로 개발계정 한도 500회의 10% 이내에 머문다. 시도별로 개별 호출하거나 측정소를 순회하는 방식 대비 결정적으로 유리하다.

**요청 파라미터 (예상)**

| 파라미터 | 값 |
|---|---|
| `serviceKey` | 발급받은 인증키 (서버 측 환경변수로만 관리) |
| `itemCode` | `PM10` / `PM25` |
| `dataGubun` | `HOUR` |
| `returnType` | `json` |
| `numOfRows`, `pageNo` | 페이징 |

### 7.2 데이터 수집 전략 (결정 사항)

**원칙: 평소에는 우리 DB를 쓰고, 외부 API는 처음 채울 때와 구멍을 메울 때만 쓴다.**

24시간 그래프를 그리려면 시간당 1개씩 총 24개의 데이터 점이 필요하다. 이를 확보하는 방식을 두 가지로 나누어 운영한다.

| 구분 | 방식 | 목적 |
|---|---|---|
| **초기 적재 (Backfill)** | 서비스 최초 구동 시, 외부 API에서 과거 24시간치를 한 번에 받아 DB에 채운다. | 배포 직후부터 그래프가 완성된 상태로 보이도록 함 |
| **정기 수집** | 이후 매시간 최신 값을 받아 DB에 누적한다. | 평상시 운영. 외부 API가 죽어도 그래프는 유지됨 |
| **공백 복구** | 배치가 실패해 DB에 빈 시간대가 생기면, 다음 성공 시 해당 구간을 과거 조회로 메운다. | 그래프에 생긴 구멍이 영구히 남는 것을 방지 |

**이 전략의 이점**

- 평상시 화면은 우리 DB에서 읽으므로 빠르고, 외부 API 장애의 영향을 받지 않는다.
- 서버가 며칠 정지했다가 재가동되어도 과거 조회로 즉시 복구된다.
- 배포 첫날 그래프가 비어 있는 문제가 사라진다.

**전제 조건**: 위 전략은 외부 API가 과거 시각 데이터를 반환한다는 가정에 기반한다. 개발 착수 시 이를 먼저 확인해야 하며(§7.6-1), **만약 최신 1건만 반환된다면 Backfill과 공백 복구는 불가능하다.** 이 경우 정기 수집만으로 운영하되, 배포 후 24시간 동안은 F-03-5에 따라 "데이터 축적 중" 안내를 노출한다.

### 7.3 등급 기준 (환경부 대기환경기준, 2018년 개정)

| 등급 | 색상 | PM10 (㎍/㎥) | PM2.5 (㎍/㎥) |
|---|---|---|---|
| 좋음 | 파랑 | 0 ~ 30 | 0 ~ 15 |
| 보통 | 초록 | 31 ~ 80 | 16 ~ 35 |
| 나쁨 | 주황 | 81 ~ 150 | 36 ~ 75 |
| 매우나쁨 | 빨강 | 151 이상 | 76 이상 |

> PM10과 PM2.5의 등급이 서로 다를 수 있다. 칩에 단일 색상을 쓸 경우 **둘 중 더 나쁜 등급**을 기준으로 삼는다. (규칙 R-01)

### 7.4 수집 배치

#### 실행 시각

수집 주기는 1시간이다. 단, **정각에 호출하지 않는다.** 측정소의 값이 에어코리아 서버에 반영되기까지 시간이 걸리므로, 정각에 호출하면 아직 갱신되지 않은 이전 시간 값을 받게 되어 그래프가 한 칸씩 밀린다.

| 시각 | 동작 |
|---|---|
| 매시 **15분** | 기본 수집 |
| 매시 **35분** | 15분 수집이 실패했거나 이전 시간 값만 수신된 경우 재시도 |
| 매시 **55분** | 최종 재시도. 이후에도 실패 시 해당 시간대를 결측으로 기록 |

**실행 주체**: Vercel 무료 플랜의 내장 크론은 하루 1회만 허용되고 실행 시각도 1시간 이내로 부정확하므로 사용하지 않는다. 대신 외부 스케줄러(cron-job.org)가 `/api/cron` 경로를 정해진 시각에 호출한다. 해당 경로는 `CRON_SECRET` 헤더 검증을 통과한 요청만 처리해 무단 호출로 인한 API 한도 소진을 방지한다.

- 3회 모두 실행되어도 일 72회로, 개발계정 한도 500회 대비 충분한 여유가 있다.
- `(sido_code, data_time)` 복합키 UPSERT를 사용하므로 중복 호출이 발생해도 데이터가 중복 적재되지 않는다.
- 15분이라는 기준값은 개발 초기 관측(§7.6-3)을 통해 조정한다.

#### 처리 흐름

```
매시 15분 (실패 시 35분, 55분 재시도)
  ↓
getCtprvnMesureLIst 호출 (itemCode=PM10)
getCtprvnMesureLIst 호출 (itemCode=PM25)
  ↓
17개 시도 × 2항목 파싱 → 결측 정규화 (§7.5 규칙 적용)
  ↓
(sido_code, data_time) 복합키로 UPSERT
  ↓
DB에 빈 시간대가 있으면 과거 조회로 공백 복구 시도
  ↓
24시간 초과 데이터도 보존(최소 30일), 화면 조회는 최근 24시간만
```

배치가 실패해도 서비스는 기존 DB 데이터로 정상 동작해야 한다. (F-04-3)

### 7.5 결측 처리 규칙

**대원칙: "측정값 0"과 "데이터 없음"은 절대 같은 것으로 취급하지 않는다.**

측정소 고장이나 점검으로 값이 없는 경우, 이를 0으로 저장하면 화면에 `0㎍/㎥ · 좋음`이 파란색으로 표시된다. 사용자는 공기가 최상이라고 믿게 되지만 실제로는 아무것도 측정되지 않은 상태다. **이는 오류 화면보다 훨씬 위험하다.** 오류는 사용자가 알아채지만, 잘못된 수치는 알아챌 수 없기 때문이다.

#### 결측 판정 기준

아래에 해당하는 값은 모두 결측(`null`)으로 정규화한다.

| 수신 값 | 처리 |
|---|---|
| `-`, `–` 등 하이픈류 | 결측 |
| 빈 문자열, 공백 | 결측 |
| `통신장애`, `점검중` 등 문자열 | 결측 |
| `null`, 필드 자체가 없음 | 결측 |
| 숫자로 변환 불가능한 값 | 결측 |
| 음수 | 결측 |
| **숫자 `0`** | **결측으로 간주** (규칙 R-02) |

> **규칙 R-02 근거**: PM10/PM2.5 농도가 실제로 정확히 0㎍/㎥으로 측정되는 경우는 사실상 없다. 따라서 0은 측정 실패 또는 초기화되지 않은 값일 가능성이 압도적으로 높다. 극히 드문 실제 0을 결측으로 처리해 잃는 손실보다, 측정 실패를 "좋음"으로 오표시해 발생하는 위험이 훨씬 크다.

#### 결측의 표시 방법

결측은 **숨기지 않고 명시적으로 드러낸다.**

| 위치 | 표시 |
|---|---|
| 칩 | 회색 처리, 수치 자리에 `–` |
| 상세 패널 | `측정 데이터 없음` 텍스트. 등급 라벨과 행동 가이드는 표시하지 않음 |
| 그래프 | 해당 구간에서 선을 끊음. 앞뒤 점을 잇거나 보간하지 않음 |
| 툴팁 | `데이터 없음` |

- 결측인 항목에는 어떤 등급도 부여하지 않는다. (색상 미부여)
- PM10만 결측이고 PM2.5는 정상인 경우, 칩 색상은 정상인 항목 기준으로 표시하되 상세에서 결측 항목을 명시한다.
- 전체 17개 시도 중 일부만 결측일 경우, 나머지 지역은 정상 표시한다.

### 7.6 개발 착수 시 확인 체크리스트

아래는 제품 결정 사항이 아니라 **개발자가 첫 구현 시 실제 API 응답으로 확인할 기술 항목**이다. 각 항목은 수십 분 내에 확인 가능하나, 확인하지 않을 경우 원인 파악에 수 시간이 소요될 수 있다.

| # | 확인 항목 | 미확인 시 증상 |
|---|---|---|
| 1 | **과거 시각 데이터 반환 여부** — `dataGubun=HOUR` 호출 시 최신 1건인지 이력 다건인지 | §7.2 전략 전체가 성립하지 않을 수 있음 |
| 2 | 응답 필드 구조 — 시도별 개별 필드인지 배열인지 | 파서 재작성 |
| 3 | 정시 이후 실제 갱신 시각 — 5분 간격 로깅으로 `dataTime` 변경 시점 관측 | 배치 시각이 부적절해 데이터가 밀림 |
| 4 | PM2.5의 `itemCode` 표기 (`PM25` / `PM2.5`) | 에러 없이 빈 결과만 반환되어 원인 추적이 어려움 |
| 5 | 결측 실제 표기값 수집 — §7.5 판정 목록에 반영 | 파싱 실패로 화면 전체 오류 |
| 6 | 세종시 데이터 제공 여부 및 결측 빈도 | 특정 칩이 상시 비어 보임 |

### 7.7 데이터 모델

```
measurements
├── id            PK
├── sido_code     VARCHAR   -- 'seoul', 'busan', ...
├── data_time     TIMESTAMP -- 측정 시각 (정시, KST)
├── pm10          INTEGER   NULL 허용  ← 결측은 반드시 NULL, 0으로 저장 금지
├── pm25          INTEGER   NULL 허용  ← 결측은 반드시 NULL, 0으로 저장 금지
├── created_at    TIMESTAMP
└── UNIQUE (sido_code, data_time)

INDEX (sido_code, data_time DESC)
```

- 결측 컬럼은 `NOT NULL` 제약이나 기본값 `0`을 설정하지 않는다. DB 레벨에서 §7.5 규칙이 깨지는 것을 방지하기 위함이다.
- 시도 코드와 한글 표기명은 코드 상수로 관리한다.

## 8. 아키텍처

```
[브라우저]
    │  GET /api/current      → 17개 시도 최신 값
    │  GET /api/history/:sido → 해당 시도 24시간 시계열
    ▼
[자체 백엔드 / API 서버]
    │  ├── 인증키 보관 (클라이언트 노출 금지)
    │  ├── 응답 캐시 (TTL 5~10분)
    │  └── 스케줄러 (매시 실행)
    ▼
[DB]  ←──  [에어코리아 Open API]
```

### 8.1 응답 전달 방식 (체감 속도 설계)

칩 클릭 시 즉각 반응하려면, **칩을 누를 때마다 서버에 묻지 않아야 한다.**

| 방식 | 동작 | 체감 |
|---|---|---|
| ❌ 지연 로딩 | 칩 클릭 → 서버 요청 → 응답 대기 → 렌더 | 클릭할 때마다 멈칫함 |
| ✅ **선(先) 적재** | 페이지 진입 시 17개 시도 전체를 1회 요청으로 수신 → 클릭은 메모리에서 처리 | 클릭 즉시 반응 |

17개 시도 × 2항목 × 24시간이라도 데이터 크기는 수십 KB 수준이므로 초기 1회 전송이 부담되지 않는다. 따라서 **`GET /api/snapshot` 한 번으로 현재값과 24시간 이력을 모두 내려주고, 이후 칩 전환은 네트워크 요청 없이 처리한다.** (F-02-5, G2 충족)

- 응답은 5~10분 캐시하며, 백그라운드에서 주기적으로 갱신한다.
- 데이터 크기가 예상보다 커질 경우, 현재값(`/api/current`)과 이력(`/api/history/:sido`)을 분리하고 이력만 지연 로딩하는 방식으로 전환한다.

### 8.2 인증키 및 호출 경로

**클라이언트는 공공 API를 직접 호출하지 않는다.** 이유:

1. `serviceKey`가 브라우저에 노출되면 키 도용으로 트래픽 한도가 소진된다.
2. 공공데이터포털 API는 CORS를 허용하지 않아 브라우저 직접 호출이 실패한다.
3. 24시간 시계열은 자체 DB에만 존재한다.

## 9. UI/UX 요구사항

### 9.1 반응형 레이아웃

| 브레이크포인트 | 칩 그리드 | 상세 패널 |
|---|---|---|
| ~640px (모바일) | 2~3열, 가로 스크롤 없이 랩 | 칩 그리드 하단에 세로 배치 |
| 641~1024px (태블릿) | 4~5열 | 하단 배치 |
| 1025px~ (데스크톱) | 6열 이상, 좌측 배치 | 우측 2단 컬럼 |

- 모바일에서 칩 탭 시 상세 영역으로 부드럽게 스크롤 이동한다.
- 터치 타깃은 최소 44×44px을 확보한다.

### 9.2 접근성

- 등급을 **색상만으로 전달하지 않는다.** 텍스트 라벨을 항상 병기한다. (색각 이상 사용자 고려)
- 칩은 키보드 포커스 및 방향키 이동이 가능해야 한다.
- 차트에는 동일 데이터를 담은 대체 텍스트 또는 표 형태의 대안을 제공한다.
- 명도 대비는 WCAG AA(4.5:1) 이상을 충족한다.

## 10. 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 초기 로드 (LCP) | 2.5초 이내 |
| 칩 전환 반응 | 200ms 이내 |
| 외부 API 호출량 | 일 50회 이하 (한도 500회 대비 10%) |
| 자체 API 캐시 | 5~10분 TTL |
| 브라우저 지원 | 최신 2개 버전의 Chrome, Safari, Edge, Firefox |
| 데이터 보존 | 최소 30일 |

## 11. 엣지 케이스

| 상황 | 처리 |
|---|---|
| 특정 시도 값이 결측 | 해당 칩만 회색 `–` 처리, 나머지는 정상 표시 |
| 외부 API 전체 장애 | 마지막 정상 데이터 + "N시간 전" 배지 |
| 서비스 최초 배포 직후 | 그래프에 "데이터 축적 중" 안내, 확보 구간만 표시 |
| 인증키 한도 초과 | 배치 로그에 기록, 캐시로 서비스 지속, 관리자 알림 |
| DST/시간대 | 모든 시각은 KST(Asia/Seoul) 고정 |
| 음수·비정상 수치 | 결측으로 간주하여 null 처리 |

## 12. MVP 범위와 로드맵

**MVP (v1.0)** — F-01 ~ F-04 전체, 17개 시도, PM10/PM2.5, 24시간 그래프, 반응형.

| 이후 버전 | 후보 기능 |
|---|---|
| v1.1 | 농도순 정렬, 지역 검색, 다크 모드 |
| v1.2 | 대기질 예보 (`getMinuDustFrcstDspth` 활용) |
| v1.3 | 시·군·구 단위 드릴다운 (`getCtprvnMesureSidoLIst`) |
| v2.0 | 즐겨찾기 + 알림, 지도 시각화, 7일 추이 |

## 13. 성공 지표

포트폴리오 목적임을 고려해 **트래픽 지표보다 완성도 지표**를 우선한다.

| 지표 | 목표 |
|---|---|
| 데이터 수집 성공률 | 주간 95% 이상 (누락 시간대 5% 미만) |
| Lighthouse 성능 / 접근성 | 각 90점 이상 |
| 배치 무중단 가동 | 30일 연속 |
| 외부 API 장애 시 화면 유지 | 100% (백지 화면 0건) |

## 14. 결정 대기 항목

| # | 항목 | 비고 |
|---|---|---|
| Q1 | 과거 시각 데이터 조회 가능 여부 (§7.6-1) | **미확인 시 §7.2 전략 전체가 흔들림. 최우선 확인 항목** |
| Q2 | 배치 시각 15분의 적정성 (§7.6-3) | 관측 후 조정 |

**v0.3에서 확정된 사항**

- 기술 스택 전체 (§15)
- 배치 실행 주체: 외부 스케줄러 cron-job.org (§7.4)

**v0.2에서 확정된 사항**

- 수집 전략: 초기 Backfill + 매시간 정기 수집 + 공백 복구 (§7.2)
- 배치 시각: 매시 15분 / 35분 / 55분 (§7.4)
- 결측 처리: 0을 결측으로 간주, 보간 없이 명시적 표시 (§7.5)
- 응답 방식: 초기 1회 전량 전송 후 클라이언트 메모리 처리 (§8.1)

## 15. 기술 스택

### 15.1 구성

| 역할 | 선택 | 비용 |
|---|---|---|
| 언어 | TypeScript | – |
| 프레임워크 | Next.js (App Router) | – |
| 스타일 | Tailwind CSS | – |
| 차트 | Recharts | – |
| 데이터베이스 | Supabase (PostgreSQL) | 무료 |
| 배포 | Vercel | 무료 |
| 스케줄러 | cron-job.org | 무료 |
| 코드 저장소 | GitHub | 무료 |

전 구성이 무료 티어 범위 내에서 운영된다.

### 15.2 선정 근거

| 선택 | 근거 |
|---|---|
| Next.js | 프론트엔드와 API 서버가 단일 프로젝트에 포함되어 개발·배포 대상이 하나로 줄어든다. 화면은 `app/page.tsx`, 수집 로직은 `app/api/cron/route.ts`에 위치한다. |
| TypeScript | 결측값(`null`) 처리가 설계 전반에 걸쳐 있는 프로젝트다(§7.5). 컴파일 단계에서 null 미처리를 검출하므로 런타임 오류를 사전에 차단한다. |
| Recharts | 데이터가 `null`인 구간에서 선을 끊어 그리는 동작이 기본값이다. F-03-4를 추가 구현 없이 충족한다. |
| Supabase | PostgreSQL 관리형 서비스. `(sido_code, data_time)` 복합 유니크 키와 UPSERT를 표준 SQL로 처리할 수 있다. |
| cron-job.org | §15.3 참조 |
| — | Next.js·Supabase 조합은 공개 레퍼런스가 풍부해 AI 코딩 도구의 산출물 정확도가 상대적으로 높다. 비전공자 단독 개발 환경에서 중요한 선정 기준으로 반영했다. |

### 15.3 스케줄러 선정

| 후보 | 판정 | 사유 |
|---|---|---|
| Vercel 내장 크론 | 부적합 | 무료 플랜은 1일 1회로 제한되며, 하위 주기 표현식은 배포 단계에서 거부된다. 실행 시각도 지정 시각으로부터 1시간 이내로 부정확하다. |
| GitHub Actions | 차선 | 무료이나 스케줄 실행이 수 분~수십 분 지연되는 사례가 잦아 §7.4의 3단 재시도 설계와 맞지 않는다. |
| **cron-job.org** | **채택** | 분 단위 정확도, 웹 UI 기반 등록·실행 이력 조회, 실패 시 알림 제공. 별도 코드 작성이 불필요하다. |

### 15.4 환경변수

| 키 | 용도 | 노출 |
|---|---|---|
| `AIRKOREA_SERVICE_KEY` | 공공데이터포털 인증키 | 서버 전용. 클라이언트 노출 금지 |
| `SUPABASE_URL` | DB 접속 주소 | 서버 전용 |
| `SUPABASE_SERVICE_ROLE_KEY` | DB 쓰기 권한 키 | 서버 전용. 절대 노출 금지 |
| `CRON_SECRET` | `/api/cron` 무단 호출 차단 | 서버 전용 |

- 모든 키는 Vercel 환경변수로 관리하며 저장소에 커밋하지 않는다. (`.env.local`은 `.gitignore` 대상)
- `NEXT_PUBLIC_` 접두사는 어떤 키에도 사용하지 않는다.

### 15.5 운영상 제약

| 항목 | 내용 | 영향 |
|---|---|---|
| Supabase 무료 플랜 | 7일간 요청이 없으면 프로젝트 일시정지 | 매시간 배치가 접속하므로 정상 운영 중에는 해당 없음 |
| Vercel 무료 플랜 | 상업적 사용 불가 | 포트폴리오 용도이므로 해당 없음 |
| cron-job.org | 외부 서비스 의존 | 중단 시 수집 정지. 재개 후 §7.2 공백 복구로 자동 보완 |

## 16. 구현 순서

데이터 파이프라인을 먼저 완성한 뒤 화면을 개발한다. **화면을 먼저 만들면 표시할 실데이터가 없어 결측·이상값 대응을 검증할 수 없다.**

| 단계 | 작업 | 완료 판정 |
|---|---|---|
| 0 | 공공데이터포털 인증키 발급 및 §7.6 체크리스트 검증 | 브라우저에서 API 호출 시 정상 응답 확인 |
| 1 | Next.js 프로젝트 생성, GitHub 저장소 연결 | 로컬 서버 구동 확인 |
| 2 | Supabase 프로젝트 생성, `measurements` 테이블 및 유니크 제약 생성 | 테이블 조회 가능 |
| 3 | `/api/cron` 구현 — 호출 → 파싱 → 결측 정규화 → UPSERT | 수동 호출 시 17개 행 적재 확인 |
| 4 | Vercel 배포 및 환경변수 설정 | 배포 URL 접속 가능 |
| 5 | cron-job.org 등록 (매시 15/35/55분) | 24시간 후 24개 시간대 적재 확인 |
| 6 | `/api/snapshot` 구현 (§8.1) | 응답 JSON 확인 |
| 7 | 화면 개발 — 칩 그리드 → 상세 패널 → 그래프 | F-01~F-04 충족 |
| 8 | 반응형·접근성 점검 (§9) | Lighthouse 90점 이상 |

3단계 완료 후 최소 24시간 데이터를 축적한 뒤 7단계를 시작할 것을 권장한다. 실제 결측 패턴을 확인한 상태에서 화면을 개발해야 §7.5 규칙의 실효성을 검증할 수 있다.

---

*본 문서는 초안이며, §7.6 체크리스트 확인 완료 후 v0.4로 갱신한다.*
